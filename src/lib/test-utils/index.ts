/**
 * Test utilities barrel export.
 *
 * Import factories and helpers from here in any test file:
 *
 * ```ts
 * import { createMockSession } from '$lib/test-utils';
 * ```
 *
 * Note: Svelte component wrappers (e.g. `context-wrapper.svelte`) must be
 * imported directly from their file paths since they are Svelte components,
 * not plain TS modules.
 */

export * from './factories';
