/**
 * Resolver stub for SvelteKit's `$app/stores` virtual module.
 *
 * See `tests/stubs/app-server.ts` for why these files exist.
 */

export const page = { subscribe: () => () => {} };
export const navigating = { subscribe: () => () => {} };
export const updated = { subscribe: () => () => {} };
